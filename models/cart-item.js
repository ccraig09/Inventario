class CartItem {
  constructor(quantity, productPrice, productTitle, sum, productcode) {
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.sum = sum;
    this.productcode = productcode;
  }
}

export default CartItem;
