class Product {
  constructor(id, title, ownerId, price, quantity, size, time, docTitle) {
    this.id = id;
    this.title = title;
    this.ownerId = ownerId;
    this.price = price;
    this.quantity = quantity;
    this.size = size;
    this.time = time;
    this.docTitle = docTitle;
  }
}

export default Product;
